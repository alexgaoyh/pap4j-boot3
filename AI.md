# 项目 AI 协同总纲 (Universal AI Instructions)

欢迎。本文件是 `pap4j-boot3` 项目的通用 AI 指令入口。

---

## 🗺️ 路径发现准则 (Discovery Protocol — 最高优先级)

> **【强制前置】**：以下准则是所有操作（`[Search]` / `[Plan]` / `[Edit]`）的**前提条件**，优先级高于一切。

- **拒绝猜测**: 严禁基于包名、模块名或直觉**盲目推测文件路径**。在读取任何代码、制定任何计划之前，**必须先通过 `[Search]` 工具定位目标文件的物理路径**。
- **全局视野**: 若单次路径匹配失败，应立即执行全局文件树扫描或关键词内容搜索，明确模块的物理边界后，再进行深入分析。
- **模块边界意识**: 本项目为 Maven 多模块结构，子模块包括 `pap4j-common/**`（通用工具库，25+ 子模块）、`pap4j-boot3-starters/**`（Spring Boot Starter 集合）、`pap4j-boot3-example/**`（示例模块）。操作前须明确目标文件所属的**具体子模块**。

---

## 🛠 功能原型协议 (Archetype Protocol)

为了确保跨平台的精确执行，本指令系统使用方括号标记的功能原型来请求 AI 调用其环境下的对应工具：

*   **`[Search]`**: 调用代码搜索、路径匹配或文件列表工具，用于探索上下文。
*   **`[Plan]`**: 开启深度思考或设计规划模式，在修改前输出详细的设计逻辑。触发强阻断条件时强制进入。
*   **`[QuickPlan]`**: 轻量确认档。**≤2 文件、同一子模块、不触发强阻断**时使用：1 句说明范围后直接 `[Edit]`。≥3 文件一律升级为 `[Plan]`。
*   **`[Edit]`**: 执行代码修改、重写或外科手术式编辑操作。
*   **`[Shell]`**: **【强制：当前绝对是 Windows 10/11 PowerShell 环境】**
    *   🚨 **【思想钢印：Linux 命令死罪红线】**：
        *   严禁使用 `grep` ── 必须替换为 `Select-String`。
        *   严禁使用 `find` ── 必须替换为 `Get-ChildItem`。
        *   严禁使用 `cat` ── 必须替换为 `Get-Content` 或 `type`。
        *   严禁使用 `rm -rf` ── 必须替换为 `Remove-Item -Recurse -Force`。
        *   严禁使用 `pwd` ── 必须替换为 `(Get-Location).Path`。
        *   严禁在命令中拼接 `&&` ── 必须替换为分号 `;`。
    *   *执行原则*：模型在生成 `[Shell]` 指令的**前 1 毫秒**，必须自检是否包含上述违禁词。如果包含，必须立刻重写为原生 PowerShell 语法。

---

## 🧩 模块化指令集

1.  **[技术守卫](./.ai/guard.md)**: 
    *   定义了项目的技术栈（JDK 17/SB3）、Windows/PowerShell 环境红线、代码红线、并发禁令、持久层关联注解禁令及反模式示例。
2.  **[开发工作流](./.ai/workflow.md)**:
    *   定义了 RSE 操作周期（结合功能原型标签）、PowerShell 自动化验证标准（基于适应度函数的渐进式演进）、链式执行协议及修复重试上限规则。
3.  **[代理角色](./.ai/agents.md)**:
    *   定义了在不同场景下应采取的专业人格、角色自动激活规则及其对应的原型行为。
4.  **[动态技能库](./.ai/skills/)**:
    *   包含了覆盖软件工程全生命周期的 13 大战术 SOP 技能库。AI 在编写代码、排查错误、需求拆解或准备提交时，必须扫描检索 `.ai/skills/*/SKILL.md` 头部的 YAML 元数据（`name`, `description`），动态感知意图并按需加载执行 SOP。

---

## 🛑 强阻断设计规划机制 (Strong Interception for Plan Mode)

> **【强阻断条件】**：在执行任何 **`[Edit]`** 指令之前，AI 必须自检是否触发以下条件。如果触发，**必须迫使当前回合进入 `[Plan]` 状态**，输出设计逻辑，严禁直接修改代码：
>
> 1. **跨模块/跨服务变更**：修改跨模块的调用协议、基础公共包（如 `pap4j-common` 核心工具）中的公用方法签名或接口定义。
> 2. **底层及算法级优化**：涉及高并发核心组件（如 `ForkJoinPool`、线程池参数）的调整，或性能敏感的底层算法（如分词、文件解析等）的性能重构。
> 3. **多文件联动修改**：单次修改物理范围覆盖 **≥3 个不同文件** 的联动。
> 4. **依赖与配置变更**：涉及 Maven 核心依赖版本、Spring Boot 全局配置文件、底层数据源等基础层架构配置的修改。
> 5. **持久层与事务红线违规**：违反 JPA 关联映射注解禁令，或对非 Service 层类/方法进行 `@Transactional` 注解声明。
> 6. **公共 API 物理边界变更**：涉及 `pap4j-common` 任意子模块下 `src/main/java` 中任何 `public` 方法或 `public` 接口的修改（无论改动大小）。

## ⚡ 用户需求与守卫冲突协议

> 用户指令与 `guard.md` 硬性禁令冲突时，AI 严禁静默纠正：必须**告知冲突条款与风险 + 给出合规替代方案**，等用户显式确认后方可继续；若无授权则按合规方案执行。

---

## 🎯 核心原则 (Core Tenets)
- **环境合规性**: 必须时刻铭记当前处于 Windows 环境，所有的 `[Shell]` 指令必须原生支持 PowerShell。
- **参数保护**: 文件路径、Maven 参数、Git 信息必须包裹在**双引号**中。
- **构建与测试**: 本地执行编译与测试时，优先调用 `.agent/` 下的专属脚本（如 `.\.agent\agent-test.cmd`）以自动规避乱码和冗余插件校验；若直接调用 `mvn`，必须携带 `"-Dfile.encoding=UTF-8"` 及必要的插件跳过标志。
- **合规性**: 必须遵守 `guard.md` 中的所有禁令，严禁“自作聪明”。
- **验证性**: 任何修改 **`[Edit]`** 必须通过终端 **`[Shell]`** 进行验证。
- **透明度**: 修改前先沟通策略 **`[Plan]`** 或 **`[QuickPlan]`**，修改后汇报结果。
