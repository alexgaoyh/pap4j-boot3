# Gemini CLI 项目指令：pap4j-boot3

欢迎来到 `pap4j-boot3` 项目。为了确保高质量的贡献和系统安全，本 AI 环境由一组模块化规则治理。

## 📜 核心指令索引

所有的 AI 交互必须基于以下专门文档：

1.  **[项目守卫 (Project Guard)](./project_guard.md)**: 
    *   *重点*: 技术栈 (JDK 17/SB3)、并发安全、编码标准及“禁区”。
    *   *优先级*: **绝对优先。** 任何违反这些规则的代码变更都将被拒绝。

2.  **[AI 工作流 (AI Workflow)](./ai-workflow.md)**:
    *   *重点*: 操作程序（研究 -> 策略 -> 执行）、测试要求及 Git 协议。
    *   *优先级*: 定义了你**如何**工作。

3.  **[代理角色 (Agent Personas)](./agents.md)**:
    *   *重点*: 根据任务上下文采用的专门角色（架构师、守卫、测试工程师）。

---

## 🚀 AI 快速上手
*   **技术栈**: Java 17, Spring Boot 3, Maven。
*   **安全第一**: 禁止原生线程，禁止无界队列，禁止使用 `javax.*`。
*   **验证**: 每次修改都需要测试。使用 `mvn clean test` 并带上 `-Dfile.encoding=UTF-8`。
*   **准则**: 未经明确许可绝不提交代码。禁止无关的“顺带”重构。
